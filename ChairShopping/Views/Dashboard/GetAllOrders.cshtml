@{
    Layout = null;
}
<head>
    <!-- Basic -->
    <meta charset="utf-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <!-- Mobile Metas -->
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no" />
    <!-- Site Metas -->
    <meta name="keywords" content="" />
    <meta name="description" content="" />
    <meta name="author" content="" />
    <link rel="shortcut icon" href="~/favicon.png" type="">
    <title>Famms - Fashion HTML Template</title>
    <!-- bootstrap core css -->
    <link rel="stylesheet" type="text/css" href="~/css/bootstrap.min.css" />
    <!-- font awesome style -->
    <link href="~/css/font-awesome.min.css" rel="stylesheet" />
    <!-- Custom styles for this template -->
    <link href="~/css/style.css" rel="stylesheet" />
    <!-- responsive style -->
    <link href="~/css/responsive.css" rel="stylesheet" />
</head>
<!-- Content Header (Page header) -->
<div class="content-header">
    <div class="container-fluid">
        <div class="row mb-2">
            <div class="col-sm-6">
                <h1 class="m-0">Orders</h1> <br />
                <a class="btn btn-success" asp-controller="dashboard" asp-action="AddOrder">Create New Order</a>
            </div><!-- /.col -->
            <div class="col-sm-6">
                <ol class="breadcrumb float-sm-right">
                    <li class="breadcrumb-item"><a href="#">Order</a></li>
                    <li class="breadcrumb-item active">Order Card</li>
                </ol>
            </div><!-- /.col -->
        </div><!-- /.row -->
    </div><!-- /.container-fluid -->
</div>
<!-- /.content-header -->

<section class="content">
    <div class="container-fluid">

        <table class="table table-hover table-striped table-bordered text-center">
            <thead class="thead-dark">
                <tr>
                    <th>Order Date</th>
                    <th>Quantity</th>
                    <th>Price</th>
                    <th>Discount</th>
                    <th>Total price</th>
                    <th>Product name</th>
                    <th>UserName</th>
                </tr>
            </thead>
            <tbody class="font-weight-bold">
                @foreach (var item in ViewBag.AllOrders)
                {
                    <tr>
                        <td>@item.OrderDate</td>
                        <td>@item.Quantity</td>
                        <td>@item.Price</td>
                        <td>@item.Discount</td>
                        <td>@item.TotalPrice</td>
                        <td>@item.Product.ProductName</td>
                        <td>@item.User.UserName</td>
                        <td><a asp-controller="dashboard" asp-action="EditOrder" asp-route-id="@item.Id" class="btn btn-primary">Update</a></td>
                        <td><a asp-controller="dashboard" asp-action="DeleteOrder" asp-route-id="@item.Id" class="btn btn-danger">Delete</a></td>
                        <td><a asp-controller="dashboard" asp-action="GetOrderDetails" asp-route-id="@item.Id" class="btn btn-success">Details</a></td>
                    </tr>
                }
            </tbody>
        </table>
        <a class="btn btn-success" asp-controller="dashboard" asp-action="index">Back</a>

        <br />
        Products count : @ViewBag.AllOrders.Count
    </div>
    <script src="~/js/jquery-3.3.1.min.js"></script>
    <!-- popper js -->
    <script src="~/js/popper.min.js"></script>
    <!-- bootstrap js -->
    <script src="~/js/bootstrap.min.js"></script>
    <!-- custom js -->
    <script src="~/js/custom.js"></script>
</section>



